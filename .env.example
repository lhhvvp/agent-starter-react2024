# Enviroment variables needed to connect to the LiveKit server.
LIVEKIT_API_KEY=<your_api_key>
LIVEKIT_API_SECRET=<your_api_secret>
LIVEKIT_URL=wss://<project-subdomain>.livekit.cloud


# Internally used environment variables
NEXT_PUBLIC_APP_CONFIG_ENDPOINT=
SANDBOX_ID=

# User management service base URL for auth proxy
# e.g. http://127.0.0.1:8081
USER_MGMT_BASE_URL=

# Development only: expose demo token returned by user management
# 1 = enabled (development only), otherwise omit or set to 0 in production
EXPOSE_DEMO_TOKEN=0

# Development only: emit additional error details to API responses and server logs
# 1 = enabled (default in non-production)
AUTH_DEBUG=1

# --- Client log ingest (optional) ---
# Forward client-side logs to Agent Worker via LiveKit Data Channel.
# Backend must enable ENABLE_CLIENT_LOG_INGEST=1 and subscribe to the same topic.
#
# Debug session toggles (recommended):
# - URL: ?debug=1 (and optional ?console=1 to relay console.*)
# - localStorage: lk_debug=1 / lk_console_relay=1
#
# Force-enable debug session via env (not recommended for production):
NEXT_PUBLIC_CLIENT_LOG_DEBUG=
# Override min level: debug|info|warn|error (default: warn; debug sessions default to debug)
NEXT_PUBLIC_CLIENT_LOG_MIN_LEVEL=
# Max payload bytes (must be <= backend CLIENT_LOG_INGEST_MAX_BYTES)
NEXT_PUBLIC_CLIENT_LOG_MAX_BYTES=32768
# Data channel topic (default: lk.client.log; backend can also accept client.log)
NEXT_PUBLIC_CLIENT_LOG_INGEST_TOPIC=lk.client.log
